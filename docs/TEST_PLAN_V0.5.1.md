# KẾ HOẠCH KIỂM THỬ V0.5.1

## TC-01 – View 00
Mở `00. Hôm nay cần làm gì?` → chọn `Tất cả dự án`.

Kỳ vọng:
- Các chỉ tiêu giữ nguyên số liệu V0.5.0.
- Có dòng chú thích giải thích việc các chỉ tiêu có thể chồng lấn.
- Bảng ưu tiên và Top 5 vẫn hoạt động như trước.

## TC-02 – TEST-HOUSE-002 / View 03
Kỳ vọng:
- Ngân sách hiển thị `3,20 tỷ`.
- Dự báo cuối kỳ hiển thị `3,28 tỷ`.
- Delta hiển thị `Vượt 80 triệu`.
- Đã thanh toán hiển thị khoảng `1,05 tỷ`.
- Phát sinh chờ duyệt 1 / 65 triệu hiển thị theo màu cảnh báo.
- Nghiệm thu còn lỗi và quá hạn khắc phục hiển thị theo màu cảnh báo.
- Kế hoạch 35%, thực tế 28%, chênh -7 điểm % giữ nguyên.

## TC-03 – Không thay dữ liệu
Mở các View 00 và 03, sau đó kiểm tra các View nghiệp vụ.
Kỳ vọng:
- Không có bản ghi nào bị sửa chỉ vì mở bảng điều hành.
- `data/deliveryos.db` giữ nguyên.
